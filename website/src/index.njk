---
layout: base.njk
title: synlynk
---

<!-- 8 section shells per Phase 1 spec: #top #carousel #relief #how #features #vision #docs #waitlist -->

<section id="top" class="section-tagline">
  <div class="section-shell">
    <div style="margin-bottom: 1rem; display: inline-flex; align-items: center; gap: 7px; border:1px solid rgba(91,141,239,0.3); background: rgba(91,141,239,0.06); border-radius:20px; padding:5px 14px; font-size:12px; font-weight:600; color: var(--blue); letter-spacing:0.5px; text-transform:uppercase;">Multi-agent coordination</div>
    <p class="hero-byline" style="color:var(--muted); max-width:640px; margin:0 auto 12px;">Most developers use one agent at a time.</p>
    <h1 class="hero-headline" style="font-size:clamp(32px,4vw,52px); font-weight:800; line-height:1.08; letter-spacing:-1.8px; max-width:760px; margin:0 auto 28px;">
      <span style="background:linear-gradient(135deg,#fff 0%,#A259F7 55%,#5B8DEF 100%); -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text;">synlynk</span> lets you run<br>a workgroup.
    </h1>
    <p style="font-size:16px;color:var(--muted);max-width:520px;margin:0 auto 28px;">Spread the work, spread the cost. Different models, different strengths, <strong style="color:var(--teal);">one shared project state</strong>.</p>
    <p style="font-size:12px; color:var(--muted);">(Phase 1 shell — full hero in Phase 2)</p>
  </div>
</section>

<section id="carousel" class="section-carousel">
  <div class="section-shell">
    <h2>Carousel shell</h2>
    <p>Terminal carousel (init / join / dispatch / status) — Phase 2 content + JS.</p>
  </div>
</section>

<section id="relief" class="section-relief">
  <div class="section-shell">
    <div class="section-label" style="font-size:12px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--blue);margin-bottom:14px;">The multi-agent unlock</div>
    <h2 class="section-title" style="font-size:clamp(28px,4vw,48px);font-weight:800;letter-spacing:-1.5px;line-height:1.1;margin-bottom:16px;">You're not limited to one agent.<br>You just needed a harness.</h2>
    <p class="section-body" style="font-size:16px;line-height:1.7;color:#6e6e73;max-width:600px;margin-bottom:48px;">Relief section (light) — context, costs, sentinel cards. Phase 2.</p>
  </div>
</section>

<section id="how" class="section-how">
  <div class="section-shell">
    <h2>How it works shell</h2>
    <p>4-step: Install → Init → Exec → Dispatch. Phase 2.</p>
  </div>
</section>

<section id="features" class="section-features">
  <div class="section-shell">
    <h2>Features shell</h2>
    <p>2x2 or grid of key features. Phase 2.</p>
  </div>
</section>

<section id="vision" class="section-vision">
  <div class="section-shell">
    <h2>Vision / OS layer shell</h2>
    <p>Isometric motherboard canvas + stats. Phase 3 (Grok canvas).</p>
  </div>
</section>

<section id="docs" class="section-docs">
  <div class="section-shell">
    <h2>Docs shell</h2>
    <p>Quickstart, reference, manual cards. Phase 2/3.</p>
  </div>
</section>

<section id="waitlist" class="section-waitlist">
  <div class="section-shell" style="text-align:center;">
    <h2>Waitlist / Early access shell</h2>
    <p>Form stub + trust line. Phase 2/3.</p>
  </div>
</section>
