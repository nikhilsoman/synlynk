# synlynk Roadmap

**Positioning:** The OS for multi-agent development.  
**Realignment spec:** `docs/superpowers/specs/2026-06-21-synlynk-roadmap-realignment-design.md`  
**Realigned:** 2026-06-21 (workgroup consensus: Claude + AGY + Codex + Nikhil)

## Version Arc

| Version | Theme | OS Layer | Status | Target |
| :--- | :--- | :--- | :--- | :--- |
| v0.1–v0.3.0 | Kernel + Filesystem | exec · telemetry · flatline · budget · project-docs ledger · enriched templates | ✅ Shipped | June 2026 |
| v0.3.1 | Sentinel + Observability | Token scraping · zombie/stall/quota/loop detection · burn rate · context bloat · sentinel severity + ack | ✅ Shipped | June 2026 |
| v0.4.0–v0.4.2 | Hybrid Workgroup + Instruction Reach + Task Status | IPC · dispatch · job store · init wizard · IDE reach · SHA manifest · drift detection · 5-state task model | ✅ Shipped | June 2026 |
| v0.5.0–v0.6.1 | Capability Engine + Job Control | Model-aware routing · 3D domain taxonomy · quality signals · SQLite WAL · constraint propagation · `synlynk story/score` | ✅ Shipped | June 2026 |
| v0.7.0 | Static Scan Quality | Language-agnostic source scanner · `## Source Architecture` injection · `synlynk scan` · 369 tests | ✅ Shipped | June 2026 |
| v0.8.0 | Support Engineer Agent | Maintainer archetype #1 · 5 signal collectors · 7/30-day dedup · foreground investigation · GH issue filing · draft fix PRs · `.agents/` config system | ✅ Shipped | June 2026 |
| **v0.8.1–v0.8.4** | Agent Fleet (deferred) | TPM · Release · Marketing Intern · PM · Docs Keeper · Security Guard — full Autopilot fleet. Requires v1.0.0 community layer (workgroup protocol, signed ledger) to be meaningful. Renumbered → v1.1.x–v1.2.x. | 📦 Deferred → v1.1–1.2 | Post-GA |
| **v0.9.0** | Kernel Fixes | Scoped context · task→file mapping · verify contract · per-agent framing · Ed25519 wired · anti-gaming baseline (sample-count cap) | ✅ Shipped | June 2026 |
| **v0.9.1** | Install Hardening + Docs Migration | Install broken after package split fixed · `_docs_dir()` configurable · `--docs-dir` flag on init · smart doc migration from existing content | ✅ Shipped | June 2026 |
| **v0.9.2** | Team Onboarding + Consensus | `synlynk join` · team digest · write-arbitration · token budgets · `synlynk decide` | ✅ Shipped | June 2026 |
| **v0.9.3** | Async Daemon | `synlynk daemon` · launchd/systemd · job queue · HTTP context server localhost:27471 | ✅ Shipped | June 2026 |
| **v0.9.4** | Context / Dispatch / Relay | SQLite-primary task state · per-agent context profiles (`.agents/<agent>.json`) · `synlynk jobs` SQLite read + `--watch` · pre-flight gate · HTTP SSE relay broker (`synlynk relay start/broadcast`) · VERIFY_SKIP sentinel · dispatch CWD + `--dangerously-skip-permissions` scoped to dispatch_flags | ✅ Shipped | June 2026 |
| ~~**v0.9.5**~~ | ~~Health Pulse~~ | Absorbed into v0.9.8 — all content (doctor, exit, repair, sync) landed in PR #70 | 📦 Retired → v0.9.8 | — |
| ~~**v0.9.6**~~ | ~~Exit + Repair + Sync~~ | Absorbed into v0.9.8 — OB-13/14/15/16/17 all shipped together | 📦 Retired → v0.9.8 | — |
| **v0.9.7** | Grok Agent Support | Grok as first-class fourth agent peer · `AGENT_CAPABILITY_BASELINES["grok"]` · GROK.md template + `_INSTRUCTION_TARGETS` · init wizard expansion · `_inject_grok_rules()` exec context injection via `--rules` · dispatch `--always-approve` fallback · `extract_tokens` nested JSON pattern · 15 new tests · 488 total | ✅ Shipped | June 2026 |
| **v0.9.8** | Health Pulse + Lifecycle | `synlynk exit` · `synlynk repair` · `synlynk sync` · `_strip_synlynk_section()` · dry-run by default · `SYNLYNK_HANDOFF.md` write · closes OB-13–17 · 13 new tests · 524 total | ✅ Shipped | Jun 2026 |
| **v0.9.9** | LIVE-1 Fixes + Harness Compatibility System (BS-14) | **Phase 1 — LIVE-1 hardening:** Agy `--non-interactive` baseline fix (caused 6h silent hang) · Grok `--always-approve` removed from dispatch path · `_check_job_stall()`: per-agent timeout + SIGKILL + `STALL_NO_OUTPUT` sentinel · `_preflight_dispatch()`: flag validation + network socket check → `HARNESS_PREFLIGHT_FAIL` sentinel · **Phase 2 — Harness Compatibility:** 5 new `state.db` tables (`harness_baselines`, `harness_records`, `harness_verb_map`, `harness_command_palette`, `harness_version_history`) · `synlynk probe` (version fingerprint → fast-path → upsert → palette scan → fence write) · `synlynk doctor` TC-1–TC-4 compliance suite · 64-row Command Interoperability Matrix · `_upsert_harness_fence()` · `HARNESS_VERSION_DRIFT` sentinel · `harness` + `model` in `.agents/<agent>.json` · PR #82 · 503 tests | ✅ Shipped | Jul 2026 |
| **v0.10.0** | **Developer Preview** | **Scope: installable + daily driver.** P0: `pyproject.toml` + `pipx install git+<url>` · first-run polish (`synlynk help`, actionable errors) · README overhaul · `synlynk migrate` (project-docs/ → state.db, git rm --cached, gitignore). P1: BS-12a agent role formalization · `synlynk release` Ship cycle stub (VERSION bump + CHANGELOG + blog stub) · `synlynk status` platform health (harness compliance + agent availability + budget pulse). P2: job completion summary (post-dispatch UX). BS-19 `synlynk launch` task picker · BS-20 deep scan pipeline · BS-21 `synlynk viz` browser dashboard. | ✅ Shipped | Jul 2026 |
| **v0.11.0** | **Agent Ecosystem Operational Layer** | `chore/modularise-init` (`__init__.py` 11K→1.5K, 5 modules extracted) · BS-16 `synlynk status` + `--json` ecosystem snapshot · TC-2 fix arc · BS-13 `synlynk watch --live` + Vizor Observatory tab · BS-22 Vizor Efficiency (R/W/T bars + cycle matrix + radar SVGs) · BS-12 Agent Autonomy Bridge (permission grants + harness config + handoff protocol + doctor wizard + SOP codification) · 868 tests | ✅ Shipped | Jul 2026 |
| **v0.12.0** | **Trust & Cost-Aware Routing** | Fable H0 gate: "every number synlynk displays is either structurally sourced or visibly labeled as an estimate." Measurement Ledger Hardening epic (#210) — provenance-tagged `cost_entries` (Phase 1, PR #236/#241/#242), per-agent structured-output adapters replacing regex token-scraping for all four agents (Phase 2, PR #244/#245 Codex, #252 Claude, #256 Agy, #257 Grok), Vizor Effort & Cost tab estimated-vs-actual flagging (#258, PR #264), and `synlynk status` surfacing `rates_updated_at` (#259, PR #266). Epic #210 fully closed 2026-07-15. See **Measurement Ledger Hardening** row below for full epic status. | ✅ Shipped | Jul 2026 |
| **v1.0.0** | **GA: Community Layer + Public Launch** | Workgroup protocol · signed capability ledger · SME archetype · game-resistant scoring · pipx/Homebrew PyPI · synlynk.com (BS-5) · Multi-repo workspace | 📋 Planned | Sep 2026 |
| **v1.1.0** | Cross-Workgroup (Team Level) | Relay → community server · cross-workgroup epics · agent entitlements | 📋 Planned | Q4 2026 |
| **v1.2.0** | Enterprise Workspace | Cross-team · org-level governance agents · enterprise entitlements | 📋 Planned | Q1 2027 |
| **v1.3.0** | Domain/Discipline Communities | Broader communities · Tokq convergence · MCP / Open Context Protocol | 📋 Planned | Q2 2027 |

## Agent Archetype Model

Four archetypes, deployed at any community level:

| Archetype | Trigger | Examples |
| :--- | :--- | :--- |
| 🔧 Maintainers | Schedule · push · CI | Support Engineer ✅ · Security Guard · Compliance Officer · Incident Responder |
| 📣 Communicators | Release · merge · schedule | Marketing Intern · Docs Keeper · Changelog Publisher · Community Manager |
| 🎯 Orchestrators | Story events · signals · budget thresholds | PM Agent · Growth Scout · Sprint Captain · Budget Sentinel |
| 🧠 SMEs | Domain tag · file path · `synlynk consult` | Security SME · Architecture SME · Performance SME · Data SME |

## Strategic Initiatives

| Initiative | Theme | Status | Spec |
| :--- | :--- | :--- | :--- |
| Synlynk Autopilot | Agent fleet (TPM, Marketing, PM, Docs, Security) as autonomous eyes & ears | 📋 Post-GA (v1.1+) — requires community layer workgroup protocol | `docs/superpowers/specs/2026-06-21-synlynk-roadmap-realignment-design.md` |
| Community Layer | Local workgroup → team → enterprise → domain communities | 📋 v1.0.0 | Same |
| Public Presence | synlynk.com · public blog (Marketing Intern) · contributor blog in GitHub | 📋 v1.0.0 | Same |
| Server-Side Strategy | Relay prototype v0.9.3 → community server v1.1 → Tokq v1.3 | 📋 Articulated, implement v1.1+ | Same |
| GitHub Projects V2 Board Sync | state.db is canonical; board is a human-readable projection. `synlynk sync --board github` pushes story status via `external_refs` table. Agents never write to the board directly. Current `--project-id` flag is a placeholder only — no bidirectional sync exists yet. Implement post-v0.10.0. | 📋 Post-v0.10.0 | memory.md: "GitHub Projects V2 — agentic-first decision" |

## Cross-Cutting Epics

Design and engineering work that spans multiple version milestones. These epics run in parallel with the version arc above — they are not tied to a single release.

| Epic | Theme | ID Range | Status | Drives |
| :--- | :--- | :--- | :--- | :--- |
| **Issue Tracking Integration** | `synlynk init`/wizard config option: file bugs/stories to GitHub Issues, `state.db`-only, or both. Auto-detect GH remote as default signal. Decouples the public-facing tracker layer (GitHub Issues — free, familiar, contributor-visible) from the internal agentic execution substrate (`state.db` — dispatch context, discipline/stage tags, capability matrix); these are complementary, not competing (synlynk's edge is the machine-readable layer underneath whatever human tracker a team already uses, not replacing GitHub). Config persisted as `issue_tracking: "github" \| "state_db_only" \| "both"` in `.synlynk/config.json`. **Reference — salvaged from #32 (closed, superseded, don't rebuild from scratch):** PR #27 (closed 2026-06-10) built `synlynk start <issue-id>` — read a GH issue, inferred agent from an `agent:` label, moved the Projects v2 board item to In Progress, launched the agent. Reusable pieces: GH Projects v2 field-ID auto-discovery with 7-day TTL cache (`.synlynk/board-cache.json`), GraphQL calls to move board items + set the Agent field, issue-body-prepended-to-`context.md` pattern, `--dry-run` full-plan preview, remote auto-detection parsing `owner`/`repo` from `git remote get-url origin` (https + git@). | — | 📋 Planned — filed 2026-07-12, not yet dispatched | `synlynk init` wizard, `.synlynk/config.json` schema, future non-GitHub trackers (Linear/Jira), `synlynk start <issue-id>` UX revival |
| **DevX** | Context-layer migration · task tracking canon · dispatch observability · usage model clarity | DevX-1–4 | ✅ Complete — shipped in v0.9.4 | v0.9.4 relay design, `generate_context()` hybrid read |
| **Command Audit** | 9-dimension command health review · stdlib OTel tracer · `synlynk telemetry` · hook wiring · autopilot trigger map | CA-0–9 | 📋 Planned — v0.9.4 scope | OTel infrastructure, CA-4/CA-5 ship with relay |
| **Agent Behaviour** | Instruction attach + compliance measurement · sentinel non-adherence patterns · per-agent variance · cross-mode adherence dataset · agent-encounters-synlynk conflict taxonomy · instruction coexistence strategy · authority framing study | AB-0–14 | 📋 AB-11/12/13 folded into BS-7 (2026-06-27); **BS-14 brainstorm pulled forward to next session (2026-06-28)** — LIVE-1 (#81) surfaced concrete harness incompatibilities (Agy stdout hang, Grok flag contamination, 6h stall no sentinel) accelerating this from pre-GA to immediate | v1.0 IDE integration argument, capability ratings, instruction coexistence |
| **Onboarding** | `synlynk doctor` · agent availability state machine · mode taxonomy rewrite · progressive init · join vs resume split · multi-repo stopgap · `synlynk exit` · `synlynk repair` · `synlynk sync` | OB-0–17 | 📋 Planned — OB-7/OB-9 patch in v0.9.4, full rewrite post-v0.9.4; OB-13/14/15 (exit+repair) and OB-16/17 (sync) in v0.9.6 | First-run success metrics, `synlynk agents` subcommand, lifecycle completeness |
| **Website Redesign** | Standalone design-first site — replace current functional/docs-dump with a product-story-led experience; visual identity, hero narrative, feature storytelling | BS-5 | 🎨 Design complete (2026-06-27) — full-page mockup + isometric motherboard diagram approved; implementation next session (~1 week) | Public launch readiness, v1.0 community layer |
| **Skill Pack Interoperability + Benchmarks** | Prove synlynk enhances (not fights) skill packs (Superpowers, GStack) · 4-round benchmark: base → Superpowers → synlynk → synlynk+Superpowers · GStack in conflict taxonomy (Phase 0 validation) · `flatline` as benchmark target · `git-drift` as coexistence enforcement artifact · Narrative: "coordination OS + domain expertise modules = better together" · Benchmark results as HN launch asset | BS-7 | 🎨 Design complete (2026-06-27) — spec: `docs/superpowers/specs/2026-06-27-bs7-skill-pack-interoperability-design.md` · Phase 0 validation + benchmark execution week of 2026-06-30 | Dev preview launch narrative; AB-11 instruction coexistence; community/plugin ecosystem |
| **Trio Orchestration Demo** | Scripted qualitative showcase: Demo 1 — state continuity across quota swap (Claude→Agy) · Demo 2 — Flatline Sentinel vs. GStack/Superpowers loop · Demo 3 — parallel dispatch + Ed25519 signed consensus · Token/cost scorecard · Proposal: `docs/proposals/demo-projects/showcase_project_proposal.md` | BS-9 | 📋 Brainstorm queued | Dev preview launch video/blog; v0.10.0 narrative |
| **PulseScape** | End-to-end product built by the agent team: glassmorphic real-time dashboard · SSE relay event stream · Ed25519 consensus panel · 6-session build across frontend (canvas/CSS), backend (FastAPI + SQLite), QA · branch safety proof · Proposal: `docs/proposals/demo-projects/end_to_end_product_proposal.md` | BS-10 | 📋 Brainstorm queued | Showcase artifact; v1.0.0 community layer demo |
| **MCP Registry Contribution** | Project Context + Todo MCP Server for `modelcontextprotocol/servers` — any MCP-compliant client (Claude Code, Cursor, Windsurf) reads/writes synlynk project state natively via tool calls · Proposal: `docs/proposals/demo-projects/public_utility_proposals.md` | BS-11 | 📋 Brainstorm queued | Ecosystem distribution; v1.0.0 community layer |
| **Public Utilities** | Standalone pip-installable tools extracted from synlynk primitives: `flatline` (loop circuit-breaker) · `git-drift` (instruction drift auditor, pre-commit hook) · `git-connectome` (codebase dep graph → self-contained HTML, feeds `synlynk viz`) · All three independently useful with zero synlynk dependency | — | 📋 Design in BS-7 (git-drift) + BS-6 (git-connectome); `flatline` standalone spec pending | Pre-launch community assets; HN/dev.to launch hooks |
| **Project Intelligence: OKF + Visualization** | Expanded scope: OKF alignment (frontmatter in project-docs, `synlynk export --okf`, interop with Obsidian/MkDocs) · Three-view viz (`synlynk viz`): **Product view** (UX screen graph, consumer-centric) · **Logical view** (component breakdown) · **Infra view** (network + cloud stack) · Cytoscape.js self-contained HTML approach (stolen from OKF viz.html) · New POV for dev onboarding — break free from code-tree-only project understanding; anyone-can-be-a-dev narrative | BS-6 | ⚠️ Superseded by BS-21 Vizor (PR #101, 2026-07-03) — `synlynk viz` shipped as local browser dashboard; OKF alignment deferred to post-v1.0 | `synlynk workspace` (v0.10.0), dev adoption, public launch narrative |
| **Harness Compatibility** | Goal: synlynk equally effective regardless of entry harness · headless execution contract per agent (stdout flushing, PTY vs pipe, network deps) · per-agent dispatch flag map in `.agents/<agent>.json` · compliance test suite · `STALL_NO_OUTPUT` sentinel · `HARNESS_PREFLIGHT_FAIL` sentinel · `HARNESS_VERSION_DRIFT` sentinel · Command Interoperability Matrix · instruction fence · palette scan | BS-14 | ✅ **Shipped** — PR #82 merged 2026-07-01 · v0.9.9 · 503 tests · blog post 34 | Cross-harness parity; LIVE-1 closed; `synlynk probe` + `synlynk doctor` TC-1–TC-4 |
| **Workspace HUD** | Cycle-aware OS dashboard — all 6 cycles (Dream · Plan · Work · Ship · Maintain · Engage) light up based on detected activity; absorbs `synlynk status` platform health panel; job write path (`synlynk jobs register/complete`, #75/#76) is the Work cycle entry point; `synlynk watch` is the terminal surface; web board shares same read-only model. | BS-13 | ✅ **Shipped** — PR #106 merged 2026-07-04 · `synlynk/hud.py` 357L · 30 tests · blog post 41. Open item: daemon HTTP enrichment for platform health panel (localhost:27471 → live budget/harness data) | `synlynk watch`, `synlynk viz`, multi-repo monitoring, `synlynk status` |
| **`__init__.py` Modularisation** | Extract probe, sentinel, upgrade, dispatch logic from `synlynk/__init__.py` (11,268L) into `probe.py`, `sentinel.py`, `upgrade.py`, `dispatch.py`. Prerequisite for BS-16. No user-facing changes. | chore | ✅ **Shipped (Pass 2)** — Pass 1 (PR landed 2026-07-01, commit `222f7da`) regrew to 10,818L by 2026-07-12 (BS-16 status, Capability Matrix, Job Lifecycle, etc. landed back into `__init__.py`). Pass 2 fixed this for good: spec `docs/superpowers/specs/2026-07-12-init-remodularization-design.md`, plan PR #179, implementation PR #180 (merged 2026-07-12/13) extracted 11 more modules (`wizard.py`, `scan.py`, `instructions.py`, `daemon.py`, `jobs.py`, `quota.py`, `context.py`, `costs.py`, `support_engineer.py`, `team.py`, `doctor.py`), bringing it to 3,406L, plus a CI guard (`7f66543`) that fails the build past 4,000L. As of 2026-07-19, normal feature work (PMA, capability sweep, quota, measurement ledger hardening, etc.) has grown it to 3,733L — comfortably under the guard, no recurrence. Issue #380 (filed against the stale 10,818L figure) closed as duplicate 2026-07-19. | Kept `__init__.py` reviewable; CI guard prevents a third regression without manual vigilance |
| **Ecosystem Status + Capacity** | `synlynk status` command (terminal + `--json`) — headless efficiency hero metric, agent fleet score, capacity table (R/W/T budgets), 6-cycle capability matrix. `harness_status` + `cycle_capability` tables in `state.db`. Probe enhancements: cycle capability write, Tier 1 capacity seeding, best-effort latest-version fetch. `_preflight_dispatch()` three new gates: CAPACITY_EXCEEDED_INPUT, CAPACITY_EXCEEDED_OUTPUT, TOOL_PRESSURE. Three dispatch modes: eco / daily-grind / perf. Headless efficiency metric. Watch panel integration (reads state.db at poll — no daemon). New `synlynk/status.py` + `HarnessSnapshot` in `hud.py`. Tier 2 (plan-aware) and Tier 3 (surge detection) deferred. Web HUD → BS-22. Prerequisite: `chore/modularise-init`. | BS-16 | 🎨 Design approved 2026-07-04 (revised) — spec: `docs/superpowers/specs/2026-07-01-bs16-ecosystem-status-design.md` · brainstorm visuals: `docs/brainstorm/bs16-ecosystem-status/` | `synlynk status` (v0.11.0), watch panel live data, dispatch routing efficiency, headless efficiency narrative |
| **Vizor Efficiency Enrichment** | Enrich existing `synlynk viz` Efficiency tab agent cards with BS-16 data: R/W/T budget bars, cycle × agent matrix, per-agent radar hexagon (SVG). Consumes `synlynk status --json`. Agy dispatch (HTML/CSS in `viz.py`). | BS-22 | ✅ **Shipped** — PR #118 merged, blog posts 46/47 (see Vizor row below; this row was left stale after that merge) | `synlynk viz` Efficiency tab, web HUD visual layer |
| **Vizor — Local Browser Dashboard** | `synlynk viz` — synlynk's **sole web HUD**. Local-first at `http://localhost:8721`; extends to synlynk.com Team/Enterprise via NATS gate/sync (post-v1.0). 5 views: Gantt (accordion drill-down, stage bars, pencil notes), User Journeys (split-pane), Architect Map (live force-directed workspace-repo graph, v2), Effort & Cost (SVG bar charts), Efficiency (agent report cards + sentinel timeline). Note system: `POST /note` → `viz-notes.json` → injected into `generate_context()`. Live JS polling + browser notifications. Zero new deps. All future web visualisation goes into Vizor — no parallel web surfaces. `synlynk status --json` is its ecosystem data contract. | BS-21 | ✅ **Shipped** — PR #101 merged 2026-07-03 · `synlynk/viz.py` 3,642L · 21 tests · blog post 39. BS-22: Efficiency tab enrichment shipped (PR #118, blog posts 46/47). Architect Map v2 shipped (PR #167, merged 2026-07-12) — replaced static tube-map SVG with live force-directed graph of `cfg["repos"]`, typed workspace-map edges, side-drawer (dispatch/Gantt-jump/GitHub/active-dream count), IDE-style file-tree sub-view; retired `--setup-tube`; blog post 53. | D1-D2 retention; ambient workspace awareness; canonical web layer for local → Team → Enterprise |
| **FTUE Onboarding + synlynk scan** | `synlynk init --wizard` — 8-screen typeform-style stdlib TUI: mandatory Phase 0 silent scan, home harness picker, workspace topology (single/mono/multi-repo), multi-repo sub-flow (2ab combined name+picker → 2c confirm), skills/plugins scan + coexistence education, agent fleet discovery, role assignment, launch cheat sheet. Commit-on-complete (Ctrl-C leaves no state). `synlynk scan` re-runnable standalone: topology detection, 14-heuristic stack fingerprinting, README+CLAUDE.md+GEMINI.md+AGENTS.md parsing, structured context.md generation, `--refresh` / `--add` / `--remove` / `--dry-run` flags. Workspace model: named `~/.synlynk/workspaces/<name>/state.db`, workspace key migration via `synlynk migrate`. Visual: S-glyph + wordmark throughout, custom robot SVG for agents, teal accent for multi-repo sub-flow. | BS-17 | 🎨 Design approved 2026-07-01 — spec: `docs/superpowers/specs/2026-07-01-bs17-ftue-onboarding-design.md` · brainstorm visuals: `docs/brainstorm/bs17-ftue-onboarding/` | `synlynk init --wizard` + `synlynk scan` (v0.10.0 P0), developer preview narrative, onboarding conversion |
| **Job Lifecycle Ground-Truth Verification** | synlynk's dispatch/job layer wrote state and trusted it was written correctly, with no independent check against reality (git, process, filesystem). Four issues, one root cause: (#128) per-job `git worktree` isolation replacing the shared invoking-shell cwd; (#129) `_reconcile_jobs()` cross-checks git state (`_inspect_worktree_git_state()`) instead of trusting a missing exit sentinel as automatic failure, new `"failed_unverified"` status, stall-killer grace-period extension on detected git activity; (#127) real `files_touched` via `git diff`/`git status` instead of hardcoded `[]`, shared `_resolve_worktree_base_commit()` helper; (#126) `cmd_migrate()` prints resolved `DB_PATH`, `_migrate_import()` fails loud (`MigrationImportError`, non-zero exit, skips destructive git cleanup) when a non-empty source lands 0 rows. Sequenced dependency order, each PR independently diff-reviewed + full-suite-verified before merge; two false-positive bugs caught and fixed via redispatch before #126 landed. | — | ✅ **Shipped** 2026-07-08 — design: `docs/superpowers/specs/2026-07-07-job-lifecycle-verification-design.md` · PRs #130, #131, #132, #133 · follow-up cleanup issue #134 (6 pre-existing/flaky CI tests, unrelated to this epic) | Dispatch job trustworthiness, job summary accuracy, migrate safety |
| **Job Lifecycle Ground-Truth Verification — Part 2 (dispatch git-finalization)** | Same root cause, one layer further out: dispatched agents (Codex/Agy) reliably do real, correct, tested work but don't reliably finish their own git steps (commit/push/open-PR), even with explicit prompting — traced to 2026-07-04, not a refactor regression. Root-cause chain: (#182) `_inspect_worktree_git_state()` didn't check `origin/<branch>` in the borrowed-worktree case, misattributing completions as 0-work; (#184) `HARNESS_INTERNAL_TIMEOUT` detected but never auto-retried, added `_retry_internal_timeout_job()` (cap 2); (#185) synlynk now performs git finalization itself — `_finalize_completed_worktree_job()`, idempotent by construction, hard exclusion list (CLAUDE.md/GEMINI.md/AGENTS.md/.cursorrules/todo.md), auto-commit/push/PR; (#198) sibling gap — reconcile could race an agent's own disk writes and permanently mislabel completed work `failed` with `0 touched`; fixed via a one-time recheck that reclassifies to `failed_unverified` (never silently promotes to `completed`). | — | ✅ **Shipped** 2026-07-12 — PRs #186, #187, #192, #197, #199 · `job-048de64b` was the first job all session where auto-finalization ran fully unattended (commit+push+PR, zero manual steps) | Dispatch reliability, no more manual git-finalization babysitting after `synlynk dispatch` |
| **`synlynk jobs` status display bug (open)** | `synlynk jobs --all` and the persisted `.synlynk/logs/<job_id>.summary` file can show stale `FAILED (exit -1)` / `0 touched` for a job whose `jobs.json` record (authoritative), git history, and GitHub PR all confirm success — found during Part 2's own review. 3 separate `_write_job_summary()` call sites in `jobs.py` (stall-check, main reconcile, daemon-queue reconciliation); the daemon-queue one hardcodes `files=[]`/`exit_code=-1` when no `.exit` file exists and is the strongest suspect for clobbering an already-correct summary, though not conclusively proven for the observed instance. | #202 | 📦 **Parked** — filed 2026-07-12, intentionally deferred; until fixed, don't trust `synlynk jobs` status display alone — cross-check `jobs.json`, `git log`/`git status` in the worktree, and `gh pr list` directly | Job status display trustworthiness |
| **Capability Matrix Hardening** | 3-stage story→agent routing: capability score (#139) → quota headroom gate (#141 base) → cost tie-break (#140). `agent_quotas` table, `QUOTA_TYPES`/`QUOTA_UNITS`, degraded-mode non-hard-block contract. Fleet dispatch scheduler follow-up (#141 optimizer): `stories.priority`/`readiness` columns, `synlynk story ready/draft` gate, `synlynk/scheduler.py` batch routing with fleet-level in-batch headroom accounting, retry/reassignment policy (`MAX_STORY_RETRIES=2`), `synlynk schedule [--execute] [--max-stories N]`. | #137–141 | ✅ **Shipped** — PR #148/#151/#152/#154/#156 merged 2026-07-11 · specs: `docs/superpowers/specs/2026-07-11-capability-matrix-hardening-design.md`, `docs/superpowers/specs/2026-07-11-fleet-dispatch-scheduler-design.md` · plan: `docs/superpowers/plans/2026-07-11-fleet-dispatch-scheduler.md` | `_best_agent_for_story` routing, `synlynk schedule` batch dispatch, fleet capacity planning |
| **Fleet Scheduler v2 (deferred)** | Reset-timing-aware bin-packing (schedule around quota reset windows, not just current headroom) · persistent quota-blocking history (surface recurring exhaustion patterns across runs, not just this batch) · GOVERNS-aware stringent readiness gate v2 (does a story's plan cover implications across all 7 SDLC stages before it's schedulable, not just a binary ready/draft label). Explicitly out of scope for v1 — gated on observing real v1 behavior first. | goal-d38e3c83 | 📦 **Deferred** — tracked as a goal, not yet a story; revisit after 30 days of v1 `synlynk schedule` production usage (2026-07-11 → 2026-08-10): review blocked-story reasons, retry/reassignment frequency, degraded-mode incidence before scoping | Fleet Dispatch Scheduler design doc's Future Work section; only convert to stories once v1 data justifies the added complexity |
| **Local Agent (5th Dispatch Agent)** | `local` dispatched as a real `aider` CLI subprocess pointed at `omlx serve` (OpenAI-compatible backend) — same worktree/log-polling/reconciliation pipeline as `claude`/`codex`/`agy`/`grok`, no bespoke HTTP driver. Model roster + hardware tiers in `.agents/local.json` (Ornith-9B pinned default, Qwen-Coder, Gemma-Coder, all 4-bit MLX quants, 16GB baseline). Zero marginal cost per job (`_model_rate_for_version` forces `agent == "local"` to 0 regardless of model). Capability envelope starts narrow by design: `synlynk local doctor` seeds two starter coordinates (`discipline=docs`, `discipline=testing`, both `stage=execute`) at a moderate calibration score so `_best_agent_for_story()` only surfaces it for small, granular tasks until real job completions widen the envelope. `_local_concurrency_exceeded()` caps in-flight local jobs by hardware (`max_concurrent`, default 1), not spend. Two-tier tests: mocked/portable in standard CI, opt-in `@pytest.mark.local_hardware` tier for real oMLX+Aider validation, excluded from CI via `-m "not local_hardware"`. Architecture revised after external review (Fable, 2026-07-12) rejected the original single-shot HTTP chat-completions design — it couldn't edit files, so there was nothing real for the capability envelope to widen from. | goal-56d4beee | ✅ **Shipped** 2026-07-13 — design: `docs/superpowers/specs/2026-07-12-local-agent-mlx-driver-design.md` · plan: `docs/superpowers/plans/2026-07-12-local-agent-mlx-driver.md` · PRs #204 (driver wiring), #205 (capability seeding + concurrency guard), #207 (real-hardware test tier), #208 (docs) · blog post 55. Role-split integration (`.synlynk/config.json` `agent_slots`, wizard role screen, CLAUDE.md Default Agent Role table) intentionally deferred as a gated follow-up (no story yet) — per 2026-07-12 decision, `local` stays out of the formal role-split surface until it has shipped in a named release and at least one real (non-mocked) job has dispatched successfully on real hardware. | `synlynk local doctor`, capability-matrix routing, zero-cost dispatch tier, pending: Named Release cut (last release v0.11.0, 2026-07-05) |
| **Measurement Ledger Hardening** | Fable H0 gate ("every number synlynk displays is either structurally sourced or visibly labeled as an estimate"). **Phase 1 — provenance-tagged cost ledger:** `cost_source`/`estimate_basis` columns on `cost_entries` (no silent `actual`, `NOT NULL` no-default enforcement), single `_insert_cost_row()` write chokepoint, `billing_mode` config, T-shirt/manual fallback chains, `[est]`/`[legacy]` CLI-label prefixes, `synlynk cost log` for unwrapped-session self-reporting. **Phase 2 — per-agent structured-output adapters, replacing regex token-scraping as primary extraction:** `extract_tokens(text, agent=)` dispatch pattern shipped for all four agents — Codex (`codex exec --json` JSONL), Claude (`-p --output-format stream-json`), Agy/Gemini, Grok — all landing `basis="structured_output"`. **Display layer:** Vizor Effort & Cost tab flags estimated-vs-actual spend per agent/stage/dream with a muted-segment + summary-card treatment (#258); `synlynk status` surfaces `rates_updated_at` from `.synlynk/model_rates.json` with an explicit staleness warning when unset (#259). | #210 | ✅ **Shipped** — Phase 1 PR #236/#241/#242 (2026-07-13) · Phase 2 adapters: Codex PR #244/#245, Claude PR #252, Agy PR #256, Grok PR #257 (2026-07-14) · Display layer: #258 PR #264, #259 PR #266 (2026-07-15). Epic #210 fully closed. Related but not in this epic's stated scope: #213 (vendor-contract probes never run against real CLIs in CI); #260 (Savings Ledger), #261 (dispatch-path unification), #262 (surface consolidation), #263 (stage constants staleness) filed as follow-ups for a later point release. | v0.12.0 (shipped 2026-07-15); Vizor cost trustworthiness; PAYG/subscription billing-mode accuracy |
| **Capability Sweep + Industry Taxonomy** | Static NAICS/APQC/SFIA lookup tables (`synlynk/taxonomy_standards.py`) plus `_migrate_db()` legacy-tag crosswalk; `synlynk capability sweep` calibration command (CLI model discovery, SFIA-scoped calibration tasks, independent cross-agent verification scoring, $10 cost guardrail); `capability_baseline.json` bundled into package releases and used to seed cold-start `capability_ratings` rows (phantom `sample_count` 3-5, `signal_source='baseline_seed'`, blended with organic signal as real jobs land); PR review-cycle multiplier (`pr_number` linkage on job/story rows, geometric decay `1.10 × 0.825^(N-1)` floored at 0.25×, applied post-hoc at `synlynk pr check` merge time, GitHub-only v1 with neutral 1.0× fallback). | — | ✅ **Shipped** — PR #367 merged 2026-07-19 (squash `86bb57a`) via Subagent-Driven Development (Codex implementer, Grok reviewer) · design: `docs/superpowers/specs/2026-07-18-capability-sweep-taxonomy-design.md` · plan: `docs/superpowers/plans/2026-07-18-capability-sweep-taxonomy.md`. Third review pass caught the baseline seed writing SFIA/APQC codes into columns real routing queries with free-text vocabulary (silently invisible to cold-start routing) plus a packaging gap (`capability_baseline.json` lived outside the installable package) — both fixed (commit `065e9d5`) before merge. 1258 tests passing. Residual non-blocking notes: `install.sh`'s remote path still installs only ~6 of ~33 package modules (pre-existing, out of scope); dual taxonomy (SFIA codes for calibration vs. free-text for live routing) left as deliberate future work. | `_best_agent_for_story` cold-start routing, `synlynk pr check` quality-signal accuracy, capability ledger seeding |
| **Payment-Model-Aware Cost Accounting** | Cost ledger now distinguishes real dollars spent from fictitious pay-as-you-go-equivalent estimates for agents on subscription or credit-grant plans. `payment_models` config section (additive, default `pay_as_you_go`); `credit_grants` ledger table; `resolve_payment_value(agent, tokens_in, tokens_out)` branching on `subscription` (marginal-overage billing past tier quota)/`credit_grant` (oldest-first multi-grant consumption)/`pay_as_you_go`; `synlynk credit grant` CLI; `costs.md` rows split into api-equivalent + actual-dollars-spent columns with mode tags (`[in-quota]`/`[overage]`/`[credit]`); `check_budgets()` Budget Pulse gained a per-agent "Payment Models" rollup (quota %, credit balance, or pay-as-you-go spend). | — | ✅ **Shipped** — PR #374 merged 2026-07-19 (squash `8596351`) via Subagent-Driven Development (Codex implementer, Grok reviewer) · design: `docs/superpowers/specs/2026-07-18-payment-model-accounting-design.md` · plan: `docs/superpowers/plans/2026-07-18-payment-model-accounting.md`. First review round found and fixed (independently, before implementation) a regression where the new `costs.md` column split would have made the budget gate read the inflated api-equivalent figure instead of real spend. A second, full-branch review round caught two further correctness bugs per-task review missed — subscription overage billed cumulative excess every call instead of marginal excess, and multi-grant credit consumption didn't chain across an agent's other unexpired grants — both fixed with regression tests before merge. 1277 tests passing. Not-in-scope follow-up (noted at design time): a "without synlynk vs. with synlynk" ROI/efficiency value display. | `check_budgets()` cumulative-spend gate accuracy; honest quota/credit/overage display for non-PAYG agents; builds on Measurement Ledger Hardening |
